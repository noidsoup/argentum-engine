// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.rna.cards

import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.DividedDamageEffect
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Biogenic Upgrade
 * {4}{G}{G}
 * Sorcery
 * Distribute three +1/+1 counters among one, two, or three target creatures, then double the number of +1/+1 counters on each of those creatures.
 */
val BiogenicUpgrade = card("Biogenic Upgrade") {
    manaCost = "{4}{G}{G}"
    colorIdentity = "G"
    typeLine = "Sorcery"
    oracleText = "Distribute three +1/+1 counters among one, two, or three target creatures, then double the number of +1/+1 counters on each of those creatures."
    spell {
        target = TargetCreature(count = 3, minCount = 1)
        effect = DividedDamageEffect(totalDamage = 3, minTargets = 1, maxTargets = 3)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "123"
        artist = "Tomasz Jedruszek"
        flavorText = "\"To survive exceptional times we must adapt by exceptional means.\"\n—Vannifar"
        imageUri = "https://cards.scryfall.io/normal/front/0/d/0dd73fb2-453f-40b9-8beb-dfa99e6a706e.jpg"
    }
}
