// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.rav.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.TimingRule


/**
 * Selesnya Signet
 * {2}
 * Artifact
 * {1}, {T}: Add {G}{W}.
 */
val SelesnyaSignet = card("Selesnya Signet") {
    manaCost = "{2}"
    colorIdentity = "WG"
    typeLine = "Artifact"
    oracleText = "{1}, {T}: Add {G}{W}."
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{1}"), Costs.Tap)
        effect = Effects.Composite(Effects.AddMana(Color.GREEN, 1), Effects.AddMana(Color.WHITE, 1))
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "270"
        artist = "Greg Hildebrandt"
        flavorText = "The symbol of the Conclave is one of unity, with tree supporting sun and sun feeding tree."
        imageUri = "https://cards.scryfall.io/normal/front/8/2/8296d86a-2c79-43c8-8a1c-1d4bb600bc6b.jpg"
    }
}
