// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.rav.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Stone-Seeder Hierophant
 * {2}{G}{G}
 * Creature — Human Druid
 * 1/1
 * Landfall — Whenever a land you control enters, untap this creature.
 * {T}: Untap target land.
 */
val StoneSeederHierophant = card("Stone-Seeder Hierophant") {
    manaCost = "{2}{G}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Human Druid"
    oracleText = "Landfall — Whenever a land you control enters, untap this creature.\n{T}: Untap target land."
    power = 1
    toughness = 1
    triggeredAbility {
        trigger = Triggers.entersBattlefield(filter = GameObjectFilter.Land.youControl(), binding = TriggerBinding.ANY)
        effect = Effects.Untap(EffectTarget.Self)
    }
    activatedAbility {
        cost = Costs.Tap
        val t = target("target", TargetPermanent(filter = TargetFilter.Land))
        effect = Effects.Untap(t)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "184"
        artist = "William Simpson"
        flavorText = "Her touch brings to bare stone what water and sun bring to rich soil."
        imageUri = "https://cards.scryfall.io/normal/front/f/4/f4e1b9f9-e58c-4474-9a31-8e5d9f96492e.jpg"
    }
}
