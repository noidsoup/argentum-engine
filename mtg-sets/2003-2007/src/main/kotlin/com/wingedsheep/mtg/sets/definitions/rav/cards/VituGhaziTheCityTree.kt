// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.rav.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.TimingRule


/**
 * Vitu-Ghazi, the City-Tree
 * Land
 * {T}: Add {C}.
 * {2}{G}{W}, {T}: Create a 1/1 green Saproling creature token.
 */
val VituGhaziTheCityTree = card("Vitu-Ghazi, the City-Tree") {
    manaCost = ""
    colorIdentity = "WG"
    typeLine = "Land"
    oracleText = "{T}: Add {C}.\n{2}{G}{W}, {T}: Create a 1/1 green Saproling creature token."
    activatedAbility {
        cost = Costs.Tap
        effect = Effects.AddColorlessMana(1)
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{2}{G}{W}"), Costs.Tap)
        effect = Effects.CreateToken(
            power = 1,
            toughness = 1,
            colors = setOf(Color.GREEN),
            creatureTypes = setOf("Saproling")
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "285"
        artist = "Martina Pilcerova"
        flavorText = "In the autumn, she casts her seeds across the streets below, and come spring, her children rise in service to the Conclave."
        imageUri = "https://cards.scryfall.io/normal/front/a/f/afc35026-a29e-4fb0-931f-5e90faf41802.jpg"
    }
}
