// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.lrw.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.ModifyStats
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter


/**
 * Imperious Perfect
 * {2}{G}
 * Creature — Elf Warrior
 * 2/2
 * Other Elves you control get +1/+1.
 * {G}, {T}: Create a 1/1 green Elf Warrior creature token.
 */
val ImperiousPerfect = card("Imperious Perfect") {
    manaCost = "{2}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Elf Warrior"
    oracleText = "Other Elves you control get +1/+1.\n{G}, {T}: Create a 1/1 green Elf Warrior creature token."
    power = 2
    toughness = 2
    staticAbility {
        ability = ModifyStats(
            powerBonus = 1,
            toughnessBonus = 1,
            filter = GroupFilter(GameObjectFilter.Permanent.withSubtype(Subtype.ELF).youControl(), excludeSelf = true)
        )
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{G}"), Costs.Tap)
        effect = Effects.CreateToken(
            power = 1,
            toughness = 1,
            colors = setOf(Color.GREEN),
            creatureTypes = setOf("Elf", "Warrior")
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "220"
        artist = "Scott M. Fischer"
        flavorText = "In a culture of beauty, the most beautiful are worshipped as gods."
        imageUri = "https://cards.scryfall.io/normal/front/7/0/706fce74-fed9-4bf7-949d-7df6bef29238.jpg"
    }
}
