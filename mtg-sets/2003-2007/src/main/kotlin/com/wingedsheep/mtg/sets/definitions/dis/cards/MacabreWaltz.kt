// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.dis.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.ForEachTargetEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget
import com.wingedsheep.sdk.scripting.targets.TargetObject


/**
 * Macabre Waltz
 * {1}{B}
 * Sorcery
 * Return up to two target creature cards from your graveyard to your hand, then discard a card.
 */
val MacabreWaltz = card("Macabre Waltz") {
    manaCost = "{1}{B}"
    colorIdentity = "B"
    typeLine = "Sorcery"
    oracleText = "Return up to two target creature cards from your graveyard to your hand, then discard a card."
    spell {
        val t = target(
            "target",
            TargetObject(optional = true, count = 2, filter = TargetFilter.CreatureInYourGraveyard)
        )
        effect = Effects.Composite(
            ForEachTargetEffect(listOf(Effects.Move(EffectTarget.ContextTarget(0), Zone.HAND))),
            Patterns.Hand.discardCards(1)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "47"
        artist = "Jim Murray"
        flavorText = "\"All dead move to the hollow rhythm of necromancy.\"\n—Savra"
        imageUri = "https://cards.scryfall.io/normal/front/d/9/d9cd7bc3-73ba-4364-84b2-9954648cd8a9.jpg"
    }
}
