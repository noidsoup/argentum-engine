// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.uds.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.DrawCardsEffect
import com.wingedsheep.sdk.scripting.effects.SearchDestination


/**
 * Yavimaya Elder
 * {1}{G}{G}
 * Creature — Human Druid
 * 2/1
 * When this creature dies, you may search your library for up to two basic land cards, reveal them, put them into your hand, then shuffle.
 * {2}, Sacrifice this creature: Draw a card.
 */
val YavimayaElder = card("Yavimaya Elder") {
    manaCost = "{1}{G}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Human Druid"
    oracleText = "When this creature dies, you may search your library for up to two basic land cards, reveal them, put them into your hand, then shuffle.\n{2}, Sacrifice this creature: Draw a card."
    power = 2
    toughness = 1
    triggeredAbility {
        trigger = Triggers.Dies
        optional = true
        effect = Patterns.Library.searchLibrary(
            filter = GameObjectFilter.BasicLand,
            count = 2,
            destination = SearchDestination.HAND,
            reveal = true
        )
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{2}"), Costs.SacrificeSelf)
        effect = DrawCardsEffect(1)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "124"
        artist = "Ray Lago"
        imageUri = "https://cards.scryfall.io/normal/front/3/2/325d9372-01c9-4e99-a966-13c8f8566e2e.jpg"
    }
}
