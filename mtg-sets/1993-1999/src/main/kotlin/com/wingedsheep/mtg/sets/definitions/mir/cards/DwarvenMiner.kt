// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mir.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Dwarven Miner
 * {1}{R}
 * Creature — Dwarf
 * 1/2
 * {2}{R}, {T}: Destroy target nonbasic land.
 */
val DwarvenMiner = card("Dwarven Miner") {
    manaCost = "{1}{R}"
    colorIdentity = "R"
    typeLine = "Creature — Dwarf"
    oracleText = "{2}{R}, {T}: Destroy target nonbasic land."
    power = 1
    toughness = 2
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{2}{R}"), Costs.Tap)
        // Shipped as TargetFilter.Land, so it destroyed basic lands too — the printed "nonbasic"
        // was dropped. Found by the Assay differential gate; see DwarvenMinerScenarioTest.
        val t = target("target", TargetPermanent(filter = TargetFilter.NonbasicLand))
        effect = Effects.Move(t, Zone.GRAVEYARD, byDestruction = true)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "169"
        artist = "Jock"
        flavorText = "\"Fetch the pestridder, Paka—we've got dwarves in the rutabagas!\"\n—Jamul, Femeref farmer"
        imageUri = "https://cards.scryfall.io/normal/front/b/7/b7142196-c379-4932-9402-97642ad2ebdb.jpg"
    }
}
