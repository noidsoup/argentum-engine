// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mir.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Armorer Guildmage
 * {R}
 * Creature — Human Wizard
 * 1/1
 * {B}, {T}: Target creature gets +1/+0 until end of turn.
 * {G}, {T}: Target creature gets +0/+1 until end of turn.
 */
val ArmorerGuildmage = card("Armorer Guildmage") {
    manaCost = "{R}"
    colorIdentity = "BRG"
    typeLine = "Creature — Human Wizard"
    oracleText = "{B}, {T}: Target creature gets +1/+0 until end of turn.\n{G}, {T}: Target creature gets +0/+1 until end of turn."
    power = 1
    toughness = 1
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{B}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.ModifyStats(1, 0, t)
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{G}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.ModifyStats(0, 1, t)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "156"
        artist = "Martin McKenna"
        flavorText = "Zhalfir's destiny rings in tempered steel.\n—Armorer Guild maxim"
        imageUri = "https://cards.scryfall.io/normal/front/e/9/e999fdc3-9269-44d7-9015-e16f5e5b73eb.jpg"
    }
}
