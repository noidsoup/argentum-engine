// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.fdn.cards

import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.DealDamageEffect
import com.wingedsheep.sdk.scripting.effects.MarkExileOnDeathEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Fiery Annihilation
 * {2}{R}
 * Instant
 * Fiery Annihilation deals 5 damage to target creature. Exile up to one target Equipment attached to that creature. If that creature would die this turn, exile it instead.
 */
val FieryAnnihilation = card("Fiery Annihilation") {
    manaCost = "{2}{R}"
    colorIdentity = "R"
    typeLine = "Instant"
    oracleText = "Fiery Annihilation deals 5 damage to target creature. Exile up to one target Equipment attached to that creature. If that creature would die this turn, exile it instead."
    spell {
        val t1 = target("t1", TargetCreature(filter = TargetFilter.Creature))
        val t2 = target(
            "t2",
            TargetPermanent(
                optional = true,
                filter = TargetFilter(GameObjectFilter.Artifact.withSubtype(Subtype.EQUIPMENT))
            )
        )
        effect = Effects.Composite(
            DealDamageEffect(5, t1),
            Effects.Exile(t2),
            MarkExileOnDeathEffect(t1)
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "86"
        artist = "Warren Mahy"
        flavorText = "\"Fireproof or your money back!\"\n—Jornald's Arms and Armor (out of business)"
        imageUri = "https://cards.scryfall.io/normal/front/5/4/54fe00aa-d284-48f9-b5a2-1bd4c5fa8e58.jpg"
    }
}
