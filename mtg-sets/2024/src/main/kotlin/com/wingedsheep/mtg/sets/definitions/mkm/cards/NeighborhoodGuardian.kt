// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mkm.cards

import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Neighborhood Guardian
 * {1}{W}
 * Creature — Unicorn
 * 2/2
 * Whenever another creature you control with power 2 or less enters, target creature you control gets +1/+1 until end of turn.
 */
val NeighborhoodGuardian = card("Neighborhood Guardian") {
    manaCost = "{1}{W}"
    colorIdentity = "W"
    typeLine = "Creature — Unicorn"
    oracleText = "Whenever another creature you control with power 2 or less enters, target creature you control gets +1/+1 until end of turn."
    power = 2
    toughness = 2
    triggeredAbility {
        trigger = Triggers.entersBattlefield(
            filter = GameObjectFilter.Creature.powerAtMost(2).youControl(),
            binding = TriggerBinding.OTHER
        )
        val t = target("target", TargetCreature(filter = TargetFilter.Creature.youControl()))
        effect = Effects.ModifyStats(1, 1, t)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "26"
        artist = "Daren Bader"
        flavorText = "A rare and beloved exception to the Agency's \"no one is above suspicion\" rule."
        imageUri = "https://cards.scryfall.io/normal/front/0/4/0438d482-b74c-4d5e-a2bc-7063c1ae73fa.jpg"
    }
}
