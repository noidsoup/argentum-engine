// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mkm.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Unauthorized Exit
 * {1}{U}
 * Instant
 * Return target nonland permanent to its owner's hand. Surveil 1. (Look at the top card of your library. You may put it into your graveyard.)
 */
val UnauthorizedExit = card("Unauthorized Exit") {
    manaCost = "{1}{U}"
    colorIdentity = "U"
    typeLine = "Instant"
    oracleText = "Return target nonland permanent to its owner's hand. Surveil 1. (Look at the top card of your library. You may put it into your graveyard.)"
    spell {
        val t = target("target", TargetPermanent(filter = TargetFilter.NonlandPermanent))
        effect = Effects.Composite(
            Effects.Move(t, Zone.HAND),
            Patterns.Library.surveil(1)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "74"
        artist = "Andreas Zafiratos"
        flavorText = "Proft had never been a by-the-book investigator, but even he never expected to find himself breaking a notorious assassin out of a maximum-security Azorius holding cell."
        imageUri = "https://cards.scryfall.io/normal/front/8/a/8a458474-d721-46d7-b487-6a47dc063cfd.jpg"
    }
}
