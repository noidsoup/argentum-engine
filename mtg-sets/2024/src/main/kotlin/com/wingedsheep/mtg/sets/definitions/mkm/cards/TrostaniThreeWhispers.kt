// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.mkm.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Trostani, Three Whispers
 * {G}{G/W}{W}
 * Legendary Creature — Dryad
 * 4/4
 * {1}{G}: Target creature gains deathtouch until end of turn.
 * {G/W}: Target creature gains vigilance until end of turn.
 * {2}{W}: Target creature gains double strike until end of turn.
 */
val TrostaniThreeWhispers = card("Trostani, Three Whispers") {
    manaCost = "{G}{G/W}{W}"
    colorIdentity = "WG"
    typeLine = "Legendary Creature — Dryad"
    oracleText = "{1}{G}: Target creature gains deathtouch until end of turn.\n{G/W}: Target creature gains vigilance until end of turn.\n{2}{W}: Target creature gains double strike until end of turn."
    power = 4
    toughness = 4
    activatedAbility {
        cost = Costs.Mana("{1}{G}")
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.GrantKeyword(Keyword.DEATHTOUCH, t)
    }
    activatedAbility {
        cost = Costs.Mana("{G/W}")
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.GrantKeyword(Keyword.VIGILANCE, t)
    }
    activatedAbility {
        cost = Costs.Mana("{2}{W}")
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.GrantKeyword(Keyword.DOUBLE_STRIKE, t)
    }
    metadata {
        rarity = Rarity.MYTHIC
        collectorNumber = "238"
        artist = "Jodie Muir"
        flavorText = "Tangled in the heart of Vitu-Ghazi, they guard their secrets from eager ears."
        imageUri = "https://cards.scryfall.io/normal/front/3/2/329f3aca-6db7-41d3-95b2-c479d14b7fa3.jpg"
    }
}
