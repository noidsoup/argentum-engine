// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.SearchDestination


/**
 * Shared Roots
 * {1}{G}
 * Sorcery — Lesson
 * Search your library for a basic land card, put it onto the battlefield tapped, then shuffle.
 */
val SharedRoots = card("Shared Roots") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Sorcery — Lesson"
    oracleText = "Search your library for a basic land card, put it onto the battlefield tapped, then shuffle."
    spell {
        effect = Patterns.Library.searchLibrary(
            filter = GameObjectFilter.BasicLand,
            destination = SearchDestination.BATTLEFIELD,
            entersTapped = true
        )
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "196"
        artist = "Alfven Ato"
        flavorText = "\"We are all living together even if most folks don't act like it. We all have the same roots, and we are all branches of the same tree.\"\n—Huu"
        imageUri = "https://cards.scryfall.io/normal/front/e/7/e7847ba5-e85e-417f-96c0-aef2e6f83994.jpg"
    }
}
