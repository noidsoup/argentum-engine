// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Pillar Launch
 * {G}
 * Instant
 * Target creature gets +2/+2 and gains reach until end of turn. Untap it.
 */
val PillarLaunch = card("Pillar Launch") {
    manaCost = "{G}"
    colorIdentity = "G"
    typeLine = "Instant"
    oracleText = "Target creature gets +2/+2 and gains reach until end of turn. Untap it."
    spell {
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.Composite(
            Effects.ModifyStats(2, 2, t),
            Effects.GrantKeyword(Keyword.REACH, t),
            Effects.Untap(t)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "189"
        artist = "Jo Cordisco"
        flavorText = "Toph hated flying. \"Launching,\" however, was a trajectory under her control."
        imageUri = "https://cards.scryfall.io/normal/front/f/1/f177e9cc-0c41-417d-9215-5ff23a1a0657.jpg"
    }
}
