// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.core.Counters
import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.effects.AddCountersEffect
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Wartime Protestors
 * {3}{R}
 * Creature — Human Rebel Ally
 * 4/4
 * Haste
 * Whenever another Ally you control enters, put a +1/+1 counter on that creature and it gains haste until end of turn.
 */
val WartimeProtestors = card("Wartime Protestors") {
    manaCost = "{3}{R}"
    colorIdentity = "R"
    typeLine = "Creature — Human Rebel Ally"
    oracleText = "Haste\nWhenever another Ally you control enters, put a +1/+1 counter on that creature and it gains haste until end of turn."
    power = 4
    toughness = 4
    keywords(Keyword.HASTE)
    triggeredAbility {
        trigger = Triggers.entersBattlefield(
            filter = GameObjectFilter.Creature.withSubtype(Subtype.ALLY).youControl(),
            binding = TriggerBinding.OTHER
        )
        effect = Effects.Composite(
            AddCountersEffect(counterType = Counters.PLUS_ONE_PLUS_ONE, count = 1, target = EffectTarget.TriggeringEntity),
            Effects.GrantKeyword(Keyword.HASTE, EffectTarget.TriggeringEntity)
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "160"
        artist = "Yosuke Adachi"
        flavorText = "Fire Lord Ozai's cruel reign only forged a stronger resolve in the hearts of the resistance."
        imageUri = "https://cards.scryfall.io/normal/front/b/a/bac81940-d717-49ff-83b2-16a22bb2c988.jpg"
    }
}
