// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity


/**
 * Kyoshi Warriors
 * {3}{W}
 * Creature — Human Warrior Ally
 * 3/3
 * When this creature enters, create a 1/1 white Ally creature token.
 */
val KyoshiWarriors = card("Kyoshi Warriors") {
    manaCost = "{3}{W}"
    colorIdentity = "W"
    typeLine = "Creature — Human Warrior Ally"
    oracleText = "When this creature enters, create a 1/1 white Ally creature token."
    power = 3
    toughness = 3
    triggeredAbility {
        trigger = Triggers.EntersBattlefield
        effect = Effects.CreateToken(
            power = 1,
            toughness = 1,
            colors = setOf(Color.WHITE),
            creatureTypes = setOf("Ally")
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "26"
        artist = "Awanqi (Angela Wang)"
        flavorText = "As long as a single Kyoshi Warrior is alive, their island remains protected."
        imageUri = "https://cards.scryfall.io/normal/front/2/1/211045e1-85c7-4088-b830-a2afa0fe520b.jpg"
    }
}
