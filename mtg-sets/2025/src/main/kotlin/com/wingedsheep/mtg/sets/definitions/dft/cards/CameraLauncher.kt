// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.dft.cards

import com.wingedsheep.sdk.core.Counters
import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.AddCountersEffect
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Camera Launcher
 * {3}
 * Artifact Creature — Construct
 * 2/2
 * Exhaust — {3}: Put a +1/+1 counter on this creature. Create a 1/1 colorless Thopter artifact creature token with flying. (Activate each exhaust ability only once.)
 */
val CameraLauncher = card("Camera Launcher") {
    manaCost = "{3}"
    colorIdentity = ""
    typeLine = "Artifact Creature — Construct"
    oracleText = "Exhaust — {3}: Put a +1/+1 counter on this creature. Create a 1/1 colorless Thopter artifact creature token with flying. (Activate each exhaust ability only once.)"
    power = 2
    toughness = 2
    activatedAbility {
        cost = Costs.Mana("{3}")
        isExhaust = true
        effect = Effects.Composite(
            AddCountersEffect(counterType = Counters.PLUS_ONE_PLUS_ONE, count = 1, target = EffectTarget.Self),
            Effects.CreateToken(
                power = 1,
                toughness = 1,
                creatureTypes = setOf("Thopter"),
                keywords = setOf(Keyword.FLYING),
                artifactToken = true
            )
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "232"
        artist = "Ben Wootten"
        flavorText = "Vnwxt's contract demanded at least one dedicated camera on him at all times."
        imageUri = "https://cards.scryfall.io/normal/front/9/6/968651db-92fb-46cd-acda-9e097668b7c9.jpg"
    }
}
