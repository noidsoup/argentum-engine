// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.TriggerBinding
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetPermanent


/**
 * Haru, Hidden Talent
 * {1}{G}
 * Legendary Creature — Human Peasant Ally
 * 1/1
 * Whenever another Ally you control enters, earthbend 1. (Target land you control becomes a 0/0 creature with haste that's still a land. Put a +1/+1 counter on it. When it dies or is exiled, return it to the battlefield tapped.)
 */
val HaruHiddenTalent = card("Haru, Hidden Talent") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Legendary Creature — Human Peasant Ally"
    oracleText = "Whenever another Ally you control enters, earthbend 1. (Target land you control becomes a 0/0 creature with haste that's still a land. Put a +1/+1 counter on it. When it dies or is exiled, return it to the battlefield tapped.)"
    power = 1
    toughness = 1
    triggeredAbility {
        trigger = Triggers.entersBattlefield(
            filter = GameObjectFilter.Creature.withSubtype(Subtype.ALLY).youControl(),
            binding = TriggerBinding.OTHER
        )
        val t = target("target", TargetPermanent(filter = TargetFilter.Land.youControl()))
        effect = Effects.Earthbend(1, t)
    }
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "182"
        artist = "Mitori"
        flavorText = "Haru hid his incredible earthbending abilities out of fear of punishment from the Fire Nation."
        imageUri = "https://cards.scryfall.io/normal/front/9/6/9657a0c0-2c26-4331-8e62-f032265a01af.jpg"
    }
}
