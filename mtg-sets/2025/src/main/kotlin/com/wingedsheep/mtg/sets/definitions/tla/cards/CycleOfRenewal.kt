// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.tla.cards

import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.SacrificeEffect
import com.wingedsheep.sdk.scripting.effects.SearchDestination


/**
 * Cycle of Renewal
 * {2}{G}
 * Instant — Lesson
 * Sacrifice a land. Search your library for up to two basic land cards, put them onto the battlefield tapped, then shuffle.
 */
val CycleOfRenewal = card("Cycle of Renewal") {
    manaCost = "{2}{G}"
    colorIdentity = "G"
    typeLine = "Instant — Lesson"
    oracleText = "Sacrifice a land. Search your library for up to two basic land cards, put them onto the battlefield tapped, then shuffle."
    spell {
        effect = Effects.Composite(
            SacrificeEffect(GameObjectFilter.Land),
            Patterns.Library.searchLibrary(
                filter = GameObjectFilter.BasicLand,
                count = 2,
                destination = SearchDestination.BATTLEFIELD,
                entersTapped = true
            )
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "170"
        artist = "Jocelin Carmes"
        flavorText = "\"These acorns are everywhere, Aang. That means the forest will grow back.\"\n—Katara"
        imageUri = "https://cards.scryfall.io/normal/front/2/3/233f4a05-057b-48f4-8f56-8d4a060a40e4.jpg"
    }
}
