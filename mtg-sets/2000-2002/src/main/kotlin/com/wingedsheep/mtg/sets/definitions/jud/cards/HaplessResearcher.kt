// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.jud.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.DrawCardsEffect


/**
 * Hapless Researcher
 * {U}
 * Creature — Human Wizard
 * 1/1
 * Sacrifice this creature: Draw a card, then discard a card.
 */
val HaplessResearcher = card("Hapless Researcher") {
    manaCost = "{U}"
    colorIdentity = "U"
    typeLine = "Creature — Human Wizard"
    oracleText = "Sacrifice this creature: Draw a card, then discard a card."
    power = 1
    toughness = 1
    activatedAbility {
        cost = Costs.SacrificeSelf
        effect = Effects.Composite(
            DrawCardsEffect(1),
            Patterns.Hand.discardCards(1)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "42"
        artist = "Ron Spears"
        flavorText = "Scholars possess such lofty knowledge that it shouldn't be surprising when they fall."
        imageUri = "https://cards.scryfall.io/normal/front/2/2/22ed0ee7-6749-4f38-8e53-c11b46b17e5d.jpg"
    }
}
