// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.ody.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.KeywordAbility


/**
 * Beast Attack
 * {2}{G}{G}{G}
 * Instant
 * Create a 4/4 green Beast creature token.
 * Flashback {2}{G}{G}{G} (You may cast this card from your graveyard for its flashback cost. Then exile it.)
 */
val BeastAttack = card("Beast Attack") {
    manaCost = "{2}{G}{G}{G}"
    colorIdentity = "G"
    typeLine = "Instant"
    oracleText = "Create a 4/4 green Beast creature token.\nFlashback {2}{G}{G}{G} (You may cast this card from your graveyard for its flashback cost. Then exile it.)"
    spell {
        effect = Effects.CreateToken(
            power = 4,
            toughness = 4,
            colors = setOf(Color.GREEN),
            creatureTypes = setOf("Beast")
        )
    }
    keywordAbility(KeywordAbility.flashback("{2}{G}{G}{G}"))
    metadata {
        rarity = Rarity.UNCOMMON
        collectorNumber = "230"
        artist = "Ciruelo"
        imageUri = "https://cards.scryfall.io/normal/front/6/a/6a5ebe27-2083-400e-8943-b506be470e3f.jpg"
    }
}
