// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.jud.cards

import com.wingedsheep.sdk.core.Counters
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.AddCountersEffect
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Ironshell Beetle
 * {1}{G}
 * Creature — Insect
 * 1/1
 * When this creature enters, put a +1/+1 counter on target creature.
 */
val IronshellBeetle = card("Ironshell Beetle") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Insect"
    oracleText = "When this creature enters, put a +1/+1 counter on target creature."
    power = 1
    toughness = 1
    triggeredAbility {
        trigger = Triggers.EntersBattlefield
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = AddCountersEffect(counterType = Counters.PLUS_ONE_PLUS_ONE, count = 1, target = t)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "121"
        artist = "Heather Hudson"
        flavorText = "\"Why waste time creating weapons? Nature provides us with all we need.\"\n—Centaur warrior"
        imageUri = "https://cards.scryfall.io/normal/front/b/e/be9299cc-6f21-4185-ac63-2fd92e843faa.jpg"
    }
}
