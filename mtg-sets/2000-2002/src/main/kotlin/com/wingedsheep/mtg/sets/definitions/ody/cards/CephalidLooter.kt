// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.ody.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Patterns
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.effects.DrawCardsEffect
import com.wingedsheep.sdk.scripting.targets.TargetPlayer


/**
 * Cephalid Looter
 * {2}{U}
 * Creature — Octopus Rogue
 * 2/1
 * {T}: Target player draws a card, then discards a card.
 */
val CephalidLooter = card("Cephalid Looter") {
    manaCost = "{2}{U}"
    colorIdentity = "U"
    typeLine = "Creature — Octopus Rogue"
    oracleText = "{T}: Target player draws a card, then discards a card."
    power = 2
    toughness = 1
    activatedAbility {
        cost = Costs.Tap
        val t = target("target", TargetPlayer())
        effect = Effects.Composite(
            DrawCardsEffect(1, t),
            Patterns.Hand.discardCards(1, t)
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "72"
        artist = "Keith Garletts"
        flavorText = "\"Amazing, isn't it, what humans will throw away when they're drowning?\""
        imageUri = "https://cards.scryfall.io/normal/front/c/b/cb6f1c4e-4fbc-4474-8dd2-5846d417b6ab.jpg"
    }
}
