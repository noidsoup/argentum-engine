// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.apc.cards

import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.effects.DealDamageEffect
import com.wingedsheep.sdk.scripting.effects.ForEachPlayerEffect
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter
import com.wingedsheep.sdk.scripting.references.Player
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Bloodfire Colossus
 * {6}{R}{R}
 * Creature — Giant
 * 6/6
 * {R}, Sacrifice this creature: It deals 6 damage to each creature and each player.
 */
val BloodfireColossus = card("Bloodfire Colossus") {
    manaCost = "{6}{R}{R}"
    colorIdentity = "R"
    typeLine = "Creature — Giant"
    oracleText = "{R}, Sacrifice this creature: It deals 6 damage to each creature and each player."
    power = 6
    toughness = 6
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{R}"), Costs.SacrificeSelf)
        effect = Effects.Composite(
            Effects.ForEachInGroup(GroupFilter(GameObjectFilter.Creature), DealDamageEffect(6, EffectTarget.Self)),
            ForEachPlayerEffect(Player.Each, listOf(DealDamageEffect(6, EffectTarget.Controller)))
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "55"
        artist = "Greg Staples"
        flavorText = "It took all its strength to contain the fire within."
        imageUri = "https://cards.scryfall.io/normal/front/5/1/518145f3-9919-4ed6-9e2e-772ee349ea57.jpg"
    }
}
