// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.pls.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.core.Subtype
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.GroupFilter
import com.wingedsheep.sdk.scripting.targets.EffectTarget


/**
 * Nemata, Grove Guardian
 * {4}{G}{G}
 * Legendary Creature — Treefolk
 * 4/5
 * {2}{G}: Create a 1/1 green Saproling creature token.
 * Sacrifice a Saproling: Saproling creatures get +1/+1 until end of turn.
 */
val NemataGroveGuardian = card("Nemata, Grove Guardian") {
    manaCost = "{4}{G}{G}"
    colorIdentity = "G"
    typeLine = "Legendary Creature — Treefolk"
    oracleText = "{2}{G}: Create a 1/1 green Saproling creature token.\nSacrifice a Saproling: Saproling creatures get +1/+1 until end of turn."
    power = 4
    toughness = 5
    activatedAbility {
        cost = Costs.Mana("{2}{G}")
        effect = Effects.CreateToken(
            power = 1,
            toughness = 1,
            colors = setOf(Color.GREEN),
            creatureTypes = setOf("Saproling")
        )
    }
    activatedAbility {
        cost = Costs.Sacrifice(GameObjectFilter.Permanent.withSubtype("Saproling"))
        effect = Effects.ForEachInGroup(
            GroupFilter(GameObjectFilter.Creature.withSubtype(Subtype.SAPROLING)),
            Effects.ModifyStats(1, 1, EffectTarget.Self)
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "85"
        artist = "John Avon"
        imageUri = "https://cards.scryfall.io/normal/front/8/c/8c6a0ca4-5006-4c9b-91cd-e01d77e4fdc2.jpg"
    }
}
