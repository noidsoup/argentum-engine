// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.apc.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Ana Disciple
 * {G}
 * Creature — Human Wizard
 * 1/1
 * {U}, {T}: Target creature gains flying until end of turn.
 * {B}, {T}: Target creature gets -2/-0 until end of turn.
 */
val AnaDisciple = card("Ana Disciple") {
    manaCost = "{G}"
    colorIdentity = "UBG"
    typeLine = "Creature — Human Wizard"
    oracleText = "{U}, {T}: Target creature gains flying until end of turn.\n{B}, {T}: Target creature gets -2/-0 until end of turn."
    power = 1
    toughness = 1
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{U}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.GrantKeyword(Keyword.FLYING, t)
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{B}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.ModifyStats(-2, 0, t)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "73"
        artist = "Darrell Riche"
        flavorText = "\"The blessings of Gaea alone are no longer enough.\""
        imageUri = "https://cards.scryfall.io/normal/front/7/e/7efe00f9-bf42-4d6f-9a22-b357b1c1e092.jpg"
    }
}
