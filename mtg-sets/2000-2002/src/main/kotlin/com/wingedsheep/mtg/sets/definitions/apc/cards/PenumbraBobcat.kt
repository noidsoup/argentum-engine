// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.apc.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity


/**
 * Penumbra Bobcat
 * {2}{G}
 * Creature — Cat
 * 2/1
 * When this creature dies, create a 2/1 black Cat creature token.
 */
val PenumbraBobcat = card("Penumbra Bobcat") {
    manaCost = "{2}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Cat"
    oracleText = "When this creature dies, create a 2/1 black Cat creature token."
    power = 2
    toughness = 1
    triggeredAbility {
        trigger = Triggers.Dies
        effect = Effects.CreateToken(
            power = 2,
            toughness = 1,
            colors = setOf(Color.BLACK),
            creatureTypes = setOf("Cat")
        )
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "82"
        artist = "Heather Hudson"
        flavorText = "This cat has two lives and it's hunting for more."
        imageUri = "https://cards.scryfall.io/normal/front/2/1/21049fee-a748-4856-99ae-3a225a168532.jpg"
    }
}
