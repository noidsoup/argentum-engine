// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.avr.cards

import com.wingedsheep.sdk.core.Keyword
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.TimingRule
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetCreature


/**
 * Slayers' Stronghold
 * Land
 * {T}: Add {C}.
 * {R}{W}, {T}: Target creature gets +2/+0 and gains vigilance and haste until end of turn.
 */
val SlayersStronghold = card("Slayers' Stronghold") {
    manaCost = ""
    colorIdentity = "WR"
    typeLine = "Land"
    oracleText = "{T}: Add {C}.\n{R}{W}, {T}: Target creature gets +2/+0 and gains vigilance and haste until end of turn."
    activatedAbility {
        cost = Costs.Tap
        effect = Effects.AddColorlessMana(1)
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{R}{W}"), Costs.Tap)
        val t = target("target", TargetCreature(filter = TargetFilter.Creature))
        effect = Effects.Composite(
            Effects.ModifyStats(2, 0, t),
            Effects.GrantKeyword(Keyword.VIGILANCE, t),
            Effects.GrantKeyword(Keyword.HASTE, t)
        )
    }
    metadata {
        rarity = Rarity.RARE
        collectorNumber = "229"
        artist = "Karl Kopinski"
        flavorText = "Its courtyards are lit during every hour of the day so that night may never fall within its walls."
        imageUri = "https://cards.scryfall.io/normal/front/9/3/939a4351-3ec7-4e6c-8cdd-766bfd670391.jpg"
    }
}
