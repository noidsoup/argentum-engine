// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.emn.cards

import com.wingedsheep.sdk.core.Zone
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.Triggers
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.GameObjectFilter
import com.wingedsheep.sdk.scripting.filters.unified.TargetFilter
import com.wingedsheep.sdk.scripting.targets.TargetObject


/**
 * Midnight Scavengers
 * {4}{B}
 * Creature — Human Rogue
 * 3/3
 * When this creature enters, you may return target creature card with mana value 3 or less from your graveyard to your hand.
 * (Melds with Graf Rats.)
 */
val MidnightScavengers = card("Midnight Scavengers") {
    manaCost = "{4}{B}"
    colorIdentity = "B"
    typeLine = "Creature — Human Rogue"
    oracleText = "When this creature enters, you may return target creature card with mana value 3 or less from your graveyard to your hand.\n(Melds with Graf Rats.)"
    power = 3
    toughness = 3
    triggeredAbility {
        trigger = Triggers.EntersBattlefield
        optional = true
        val t = target(
            "target",
            TargetObject(
                filter = TargetFilter(GameObjectFilter.Creature.ownedByYou().manaValueAtMost(3), zone = Zone.GRAVEYARD)
            )
        )
        effect = Effects.Move(t, Zone.HAND)
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "96"
        artist = "Jason Felix"
        imageUri = "https://cards.scryfall.io/normal/front/5/a/5a57187c-f0ec-40de-b725-786f787b00a2.jpg"
    }
}
