// === GENERATED by mtgish-tooling from the mtgish oracle IR (predictive, approximate). ===
// Complete render — no manual wiring needed. Still review the rules text and add a passing
// scenario test before relying on it.

package com.wingedsheep.mtg.sets.definitions.woe.cards

import com.wingedsheep.sdk.core.Color
import com.wingedsheep.sdk.dsl.Costs
import com.wingedsheep.sdk.dsl.Effects
import com.wingedsheep.sdk.dsl.card
import com.wingedsheep.sdk.model.Rarity
import com.wingedsheep.sdk.scripting.TimingRule


/**
 * Rootrider Faun
 * {1}{G}
 * Creature — Satyr Scout
 * 1/3
 * {T}: Add {G}.
 * {1}, {T}: Add one mana of any color.
 */
val RootriderFaun = card("Rootrider Faun") {
    manaCost = "{1}{G}"
    colorIdentity = "G"
    typeLine = "Creature — Satyr Scout"
    oracleText = "{T}: Add {G}.\n{1}, {T}: Add one mana of any color."
    power = 1
    toughness = 3
    activatedAbility {
        cost = Costs.Tap
        effect = Effects.AddMana(Color.GREEN)
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    activatedAbility {
        cost = Costs.Composite(Costs.Mana("{1}"), Costs.Tap)
        effect = Effects.AddManaOfChoice()
        manaAbility = true
        timing = TimingRule.ManaAbility
    }
    metadata {
        rarity = Rarity.COMMON
        collectorNumber = "182"
        artist = "Jesper Ejsing"
        flavorText = "\"Human feet aren't inferior to hooves for climbing. It's their plodding, mistrustful personalities that hold them back.\""
        imageUri = "https://cards.scryfall.io/normal/front/4/e/4e87db5a-1a70-42df-83a2-c0f71fe8533a.jpg"
    }
}
